 A DNS server which provides load balancing by returning different IP addresses for the same Domain
 
Tools: Python, Socket,dnslib(DNS packet handling),Load Balancing: random.choice(),Testing:nslookup,Port Checking:.netstat, Vs Code

∗ Developed A custom DNS Load Balancer for distributing traffic across multiple servers, improving
resource allocation and availability.

∗ Implemented dnslib for efficient DNS query handling, achieving faster response times and better fault
tolerance.

∗ Created a dynamic DNS resolver that returns different IPs for the same domain, ensuring optimized load
balancing and redundancy.

∗ Applied network debugging techniques using netstat and nslookup to analyze server responsiveness and
troubleshoot DNS resolution issues.
